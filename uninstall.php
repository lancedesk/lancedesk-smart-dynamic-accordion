<?php
/**
 * Plugin uninstall handler.
 *
 * @package LanceDeskSmartDynamicAccordion
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}

// Cleanup stub:
// This plugin currently stores no persistent options or custom tables.
// Add delete_option() calls here when persistent settings are introduced.
